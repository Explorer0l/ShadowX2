aiogram==3.15.0
aiohttp==3.9.5
python-dotenv==1.0.1

# Optional AI moderation extras (install only if AI needed)
## To enable AI, set env AI_PROFANITY_ENABLED=1
transformers==4.43.3
huggingface-hub==0.24.6
detoxify==0.5.1
langid==1.1.6
# CPU-only PyTorch suitable for VPS
torch==2.4.1

# Windows: avoid non-stdlib 'regex' (we use Python re)
